import{_ as l,p as i,b as d,c as h,l as p,a8 as c,e as t,w as n,d as a,a9 as u,k as s,aa as m,ab as w,ac as g,ad as f,ae as y}from"./chunks/framework.Yp4hj_cn.js";const U=JSON.parse('{"title":"Building a GPU in 24 hours and winning HackMIT","description":"GPUs are expensive. We are broke. So, we built one. Why not?","frontmatter":{"title":"Building a GPU in 24 hours and winning HackMIT","description":"GPUs are expensive. We are broke. So, we built one. Why not?","longDescription":"","date":"2026-09-23T00:00:00.000Z","hidden":false},"headers":[],"relativePath":"blogs/hackmitgpu/index.md","filePath":"blogs/hackmitgpu/index.md"}'),b={name:"blogs/hackmitgpu/index.md"};function k(v,e,I,T,P,x){const o=i("BlogImage"),r=i("BlogRow");return d(),h("div",null,[e[6]||(e[6]=p('<p><img src="'+c+`" alt="alt text"></p><h2 id="pulling-the-impossible" tabindex="-1">Pulling the Impossible <a class="header-anchor" href="#pulling-the-impossible" aria-label="Permalink to “Pulling the Impossible”">​</a></h2><p>If I&#39;m being honest, I still can&#39;t believe we managed to pull it off. Over the course of 24 hours, we built a GPU, made kernels for it, and were able to demo it live, eventually winning second place at HackMIT.</p><p>One might say, how is this possible? It takes AMD and NVIDIA at least a year to build a new GPU. Well, you see, we made an incredibly simple GPU, essentially an MVP GPU. It also helps that we spend most of our waking hours working on chips and had the objectively best team: Liam Hanrahan, Yolanda Hu, Kshemaahna Nagi, and me. Additionally, we had the power of &quot;<strong>Pure Protein Chocolate Peanut Butter Bars</strong>&quot; by our side. This is not a paid sponsorship. I think I ate 5 over a 24 hour period. I only stopped because I ran out.</p><p>Before I go too deep, you should know that Liam also wrote a post on our GPU, which can be found <a href="https://www.outercloud.dev/blogs/gpu/" target="_blank" rel="noreferrer">here</a>. I plan to go a bit deeper into the architecture and the process of building it, and hope that this post reflects what little of my sanity was left at the end of the hackathon.</p><h2 id="pre-game" tabindex="-1">Pre Game <a class="header-anchor" href="#pre-game" aria-label="Permalink to “Pre Game”">​</a></h2><p>Why build a GPU? Well, Liam and I were sitting at a table and talking about how we didn&#39;t want to vibecoding for hours on end. It just didn&#39;t feel fulfilling. So we thought &quot;what&#39;s the most ridiculous thing our team would be uniquely good at building. Over the summer, Liam and I had always discussed plans to build a GPU at some point. However, we could never find time. Naturally, we put two and two together and wondered, what if we built a GPU in 24 hours? We pitched the idea to our team and decided to pursue it.</p><p>Frankly, I worried it couldn&#39;t be done. In all of the iterations of our CPU and all my personal projects, we&#39;ve never been able to go from 0-1 that fast. Naturally, this just meant we had to plan really really well.</p><p>Before the hack, we made a plan. We drew out the architecture on our lounge whiteboard and assigned rough roles. Liam and I would work on the RTL for the GPU itself while Yolanda and Kshemaahna would write kernels to run on our GPU. Sadly, the original diagram has been lost to time (we erased our whiteboard).</p><p>Our plan for the GPU was to build it in 4 parts. First, start off with a simple RV32 pipelined core. Second, vectorize it to make it into a <code>SIMD</code> (Single Instruction Multiple Data) machine. Third, add hardware multithreading (called warps in NVIDIA GPUs and waves in AMD). Fourth, build kernels and implement multicore cache coherence.</p><h2 id="the-start" tabindex="-1">The Start <a class="header-anchor" href="#the-start" aria-label="Permalink to “The Start”">​</a></h2><p>The day of the hackathon started off with a &quot;<strong>Pure Protein Chocolate Peanut Butter Bar</strong>&quot;. The dining hall wasn&#39;t open and I was hungry. The opening ceremony was great and I loved 3blue1brown&#39;s speech. After that it was hacking time!</p><p>We naturally started hacking by getting lunch. Food is very important. After lunch, we got to work. Liam decided to take up adding warps to our design while I took on vectorizing our design. Kshemaahna decided to make an LLM run on our GPU while Yolanda took on a physics sim.</p><p>One problem was that we didn&#39;t have enough time to implement warps and vectorization sequentially, so we had to make them on separate branches and merge them later. (Insert subtle foreshadowing about a merge conflict from hell here)</p><h2 id="vectorization" tabindex="-1">Vectorization <a class="header-anchor" href="#vectorization" aria-label="Permalink to “Vectorization”">​</a></h2><h3 id="v1" tabindex="-1">V1 <a class="header-anchor" href="#v1" aria-label="Permalink to “V1”">​</a></h3><p>How do you turn a CPU that executes one instruction on one piece of data every cycle into one that executes one instruction on <code>n</code> pieces of data at once? The solution is copy-paste. Yes, just take the pipeline and duplicate it <code>n</code> times. The first vectorized version read the instruction and decoded it like normal. Then, it split into 16 register files, 16 ALUs, and a 16-wide writeback stage. This works for most problems, but has an issue with memory. We didn&#39;t have 16 DDR4 chips, so we could only make one memory request a cycle. This was resolved by only accepting the memory request from the first lane.</p><p>If something seems off to you, you&#39;re right. While this version is vectorized and runs 16 computations at once, it does the same 16 computations at once every single time. So, it&#39;s pretty worthless. However, it is technically correct and we were able to run mandelbrot at the exact same speed a single-lane CPU would have. We could even run DOOM.</p><h3 id="v2" tabindex="-1">V2 <a class="header-anchor" href="#v2" aria-label="Permalink to “V2”">​</a></h3><p>So how do you make vectorization useful? The best way would be to run a different program on every lane. However, this is just a bad multicore CPU (or if done well a good SIMT machine). What we need is a way for each lane to run the same instruction on a unique piece of data. This required our first custom instruction <code>laneid rd</code>. It simply writes the lane id to the target register. This can be used like so:</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki github-light" style="background-color:#fff;color:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="color:#24292E;">data </span><span style="color:#D73A49;">=</span><span style="color:#E36209;"> arr</span><span style="color:#24292E;">[offset </span><span style="color:#D73A49;">+</span><span style="color:#6F42C1;"> laneid</span><span style="color:#24292E;">()];</span></span></code></pre></div><p>This lets each lane do unique work but also introduces a few unique problems:</p><ol><li>Our memory system is now broken; it can&#39;t accept 16 requests at once</li><li>What happens when lanes take different branches?</li></ol><p>If you remember from the <a href="https://armaangomes.com/blogs/techooo/" target="_blank" rel="noreferrer">previous blog</a>, the last memory system I wrote was a non-blocking data cache. This lets us continue executing instructions while the memory is returning the correct value. If I&#39;m being honest, this was written in an incredibly poor way and was very fragile. The core fragility was that most memory ops would have &gt;=1 cycle latency while MMIO reads were combinational. Naturally, this relied on a very jank handshake bypass signal. If this sounds like really bad design, it is. It&#39;s awful.</p><p>After fixing all of those, and making all memory operations non-blocking and decoupled, I then implemented a simple state machine in our execute stage to stall and serialize the memory requests. This was again a bad decision, but wasn&#39;t a problem for now. (More foreshadowing)</p><p>As for branching, I had a very simple solution: force all lanes to take the same branch. What this really means is that if you ever had a branch that depended on per-lane data, the GPU would explode. This might sound incredibly stupid, but with enough compiler torture you can write GPU accelerated kernels that don&#39;t branch.</p><p>This led to our first actual GPU accelerated kernel: mandelbrot. However, now instead of doing 1 pixel 16 times, we could do 16 pixels at once. This led to an almost <strong>16x bump in performance</strong>. However, due to memory serialization and the lack of branches, some performance was lost.</p><h3 id="v3-branches-and-bs" tabindex="-1">V3 Branches and BS <a class="header-anchor" href="#v3-branches-and-bs" aria-label="Permalink to “V3 Branches and BS”">​</a></h3><p>Now, how do we branch without exploding? Masks. The pattern is simple. When the programmer wants to branch, perform the following steps:</p><ol><li>Compute the condition for every branch</li><li>Determine which lanes have a true/false condition</li><li>Mask out the branches that don&#39;t take the branch</li><li>Run true branch</li><li>Flip mask</li><li>Run false branch</li><li>Disable mask</li></ol><p>Once again this seems convoluted and painful. It is; ideally we could handle branch divergence in hardware, but there was not enough time to implement that. How does this work in hardware? First, I added a lane mask controller (<code>LMC</code>). This is used as a stamp on each instruction that can toggle on/off ALUs, memory operations, and register writeback on a per-lane basis. It&#39;s stored as an <code>n</code>-bit register.</p><p>This necessitates a few extra instructions. We first need an instruction to set the <code>LMC</code> (<code>lmc</code>) so the program can turn specific lanes on and off. Then we need a way to compare boolean conditions across lanes. I took inspiration from the Vortex GPU for this and implemented a <code>ballot</code> instruction. <code>ballot</code> checks if a given register is 0 for every lane and loads its result into the target register.</p><p>This lets us translate:</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki github-light" style="background-color:#fff;color:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="color:#D73A49;">if</span><span style="color:#24292E;"> (a </span><span style="color:#D73A49;">==</span><span style="color:#24292E;"> b) {</span></span>
<span class="line"><span style="color:#D73A49;">  do</span><span style="color:#24292E;">(c);</span></span>
<span class="line"><span style="color:#24292E;">} </span><span style="color:#D73A49;">else</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#D73A49;">  do</span><span style="color:#24292E;">(d);</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><p>into:</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki github-light" style="background-color:#fff;color:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="color:#24292E;">e </span><span style="color:#D73A49;">=</span><span style="color:#6F42C1;"> ballot</span><span style="color:#24292E;">(a </span><span style="color:#D73A49;">==</span><span style="color:#24292E;"> b)</span></span>
<span class="line"><span style="color:#6F42C1;">lmc</span><span style="color:#24292E;">(e)</span></span>
<span class="line"><span style="color:#D73A49;">do</span><span style="color:#24292E;">(c)</span></span>
<span class="line"><span style="color:#6F42C1;">lmc</span><span style="color:#24292E;">(</span><span style="color:#D73A49;">~</span><span style="color:#24292E;">e)</span></span>
<span class="line"><span style="color:#D73A49;">do</span><span style="color:#24292E;">(d)</span></span></code></pre></div><p>While this is annoying, it lets us handle branching with only slight nonsense. Note that this paradigm allows normal branching provided the branches don&#39;t deal with lane-specific data.</p><h2 id="warps" tabindex="-1">Warps <a class="header-anchor" href="#warps" aria-label="Permalink to “Warps”">​</a></h2><p>While I was working on vectorization, Liam worked on warps. The core goal of warp scheduling is to leverage thread level parallelism during long stall periods. For example, when running DOOM, our average memory access time (<code>AMAT</code>) is between 5 and 9 cycles. When we vectorize our processor and run 16 memory operations in series, that&#39;s almost 128 cycles of waiting while doing nothing. This is further exacerbated when we can have up to 64 memory operations in flight at once.</p><p>The GPU can maintain up to 4 simultaneous warps. A warp contains all of the execution context required for 1 thread (later expanded to 16 with vectorization). When a warp is running and is forced to stall on a memory load, it instead swaps to the next warp. This requires us to flush the core, which costs ~5 cycles. If we swap on every single memory operation this is inefficient, but as we vectorize, the benefits grow.</p><p>When managing warps, we could use the <code>warpid</code> and <code>terminate</code> instructions to manage dataflow.</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki github-light" style="background-color:#fff;color:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="color:#24292E;">data </span><span style="color:#D73A49;">=</span><span style="color:#E36209;"> arr</span><span style="color:#24292E;">[offset </span><span style="color:#D73A49;">+</span><span style="color:#6F42C1;"> warpid</span><span style="color:#24292E;">()];</span></span></code></pre></div><p>To manage and maintain warps, we used a separate 32-register file for each hardware warp. We also implemented subsystems to define more than 4 warps in software, a <code>warpid</code> instruction (just like the <code>laneid</code> instruction), and a way to terminate and launch new warps as hardware warp slots freed up. Once we merged warps and vectorization, they would let us define kernels in the same block/thread format as CUDA. There was initially a problem where terminating a warp did not wipe the register file, meaning that the next warp would init with malformed registers. This was solved in software by clobbering the reg file before terminating.</p><h2 id="the-merge-conflict-from-hell" tabindex="-1">The Merge Conflict from Hell <a class="header-anchor" href="#the-merge-conflict-from-hell" aria-label="Permalink to “The Merge Conflict from Hell”">​</a></h2><p>Once Liam and I had developed each of these features, we had to merge them. This was a slight nightmare. Most files were relatively straightforward, though some, like Execute.scala, had misaligned changes which made sewing it back together take a while. All in all, the merge took 2 hours.</p><p>However, that didn&#39;t mean everything worked now. We had changes that didn&#39;t conflict in terms of lines of code but instead in intent. For example, serializing the memory for vectorization blocked the execute. However, in the warp branch, the hardware multithreading relied on non-blocking memory to switch threads. This required a dedicated Asynchronous Load Store Unit. This unit takes the parallel memory load and asynchronously serializes it, freeing up the execute to run another thread.</p><p>The register file also needed some changes. In the original core, it was a simple combinational register file with a massive mux between registers. This was fine with a 32-register file. Now we have 2048 registers. This would require 65k muxes for each read port, which is a bit ridiculous. Instead, I modified the register file to use SyncReadMem. This introduces new challenges. The standard reg file has 2 read ports and 2 write ports (the second write is for async memory writes). But with SyncReadMem, going over 2 ports is a bad idea.</p><p>This was a rather confusing problem for my brain at 3 am. Eventually, I realized I could use the power of metadata to save the day. The strategy revolves around having 2 banks, each 2048 registers large. Then, on writes, the register file tracks where the latest write to each register in the bank is. Then, on a read you can read from the bank with the latest value. This is called an <code>LVT</code> (Live Value Table). This should be able to run with just 2 ports per bank if muxed correctly. I actually did it slightly incorrectly so it synthesized as LUTRAM instead of BRAM, which is unfortunate, but it still ran, so all&#39;s well that ends well.</p><p>In this integration process, I added two more instructions, warp spawn (<code>wspawn</code>) and barrier (<code>bar</code>). The former is used to dynamically allocate warps and spawn new ones on command while the latter is for cross-warp synchronization. It forces warps to wait until every other warp has also reached the barrier. This is useful for framesync and stops tearing.</p><p>From what I recall, these were the biggest changes with the merge, though the longer the night drew on the less I remember. Also at this point I was on my fourth &quot;<strong>Pure Protein Chocolate Peanut Butter Bar</strong>&quot;. It was also 4 am.</p>`,50)),t(o,{caption:"Architecture Diagram"},{default:n(()=>[...e[0]||(e[0]=[a("p",null,[a("img",{src:u,alt:""})],-1)])]),_:1}),e[7]||(e[7]=a("h2",{id:"the-gpu-runs",tabindex:"-1"},[s("The GPU Runs "),a("a",{class:"header-anchor",href:"#the-gpu-runs","aria-label":"Permalink to “The GPU Runs”"},"​")],-1)),e[8]||(e[8]=a("p",null,"At this point, I needed to get it running on the FPGA. Unfortunately, every Vivado synthesis took 30 minutes, so debugging the scaffold took quite a while. In the meantime, I pointed Claude at our simulation and set up a small environment for it to build and test kernels. As much as we would have loved to write all the kernels ourselves, we simply didn't have the time. Eventually we got it running on our massive TV with only a few timing violations and were able to run a full mandelbrot kernel with 64 concurrent threads.",-1)),t(o,{caption:"Timing Violations Led To Unique Images"},{default:n(()=>[...e[1]||(e[1]=[a("p",null,[a("img",{src:m,alt:""})],-1)])]),_:1}),e[9]||(e[9]=a("p",null,[s('While all of this was running, Claude was used to generate some simple 3D rendering, fluid simulation, and physics simulation demos. Again, it was like 5 am. Liam was also working on writing kernels at the same time. Once we got the demos working, I had my fifth and final "'),a("strong",null,"Pure Protein Chocolate Peanut Butter Bar"),s('" at this point. It was also 8 am so we rushed off to the venue.')],-1)),e[10]||(e[10]=a("h2",{id:"judging",tabindex:"-1"},[s("Judging "),a("a",{class:"header-anchor",href:"#judging","aria-label":"Permalink to “Judging”"},"​")],-1)),e[11]||(e[11]=a("p",null,"While we were waiting for judging to start, we updated the plume, made a simple set of slides, and set up the demo. During this time we also fixed a few bugs in the register file and worked to integrate Kshemaahna's LLM code and run DOOM. You might wonder, how do you run a game intended to run on a single thread on a CPU on a 64-thread GPU? Well, we just turned 63 threads off. Yes, this is goofy, but we simply didn't have time to make it work better (it was already hitting 20-30 fps). On a positive note, we got DOOM running, and then proceeded to work on adding the LLM Demo. This was honestly my favorite demo and Kshemaahna did some insane work on it.",-1)),t(o,{caption:"DOOM Running on the GPU at Judging"},{default:n(()=>[...e[2]||(e[2]=[a("video",{src:w,controls:"",style:{width:"60%","border-radius":"8px"}},null,-1)])]),_:1}),e[12]||(e[12]=a("p",null,"Eventually we spoke to judges and a bunch of our fellow competitors. It was awesome meeting all the amazing people at HackMIT and we had a ton of fun. At the end, we were invited to panel judging for a second round. Around 4, we headed up to the panel and presented our GPU. It was pretty terrifying, especially considering that we were given 7 minutes to present without any setup time. Thus, I spent most of the presentation fumbling while trying to speed run setting up the FPGA and flashing it (programming the FPGA and flashing DOOM takes 2 minutes). In the end, though, we gave a good presentation and headed back down to wait for the awards ceremony.",-1)),t(r,null,{default:n(()=>[t(o,{caption:"GPU Running DOOM"},{default:n(()=>[...e[3]||(e[3]=[a("p",null,[a("img",{src:g,alt:""})],-1)])]),_:1}),t(o,{caption:"The Instagram Recap"},{default:n(()=>[...e[4]||(e[4]=[a("video",{src:f,controls:""},null,-1)])]),_:1})]),_:1}),e[13]||(e[13]=a("h2",{id:"awards",tabindex:"-1"},[s("Awards "),a("a",{class:"header-anchor",href:"#awards","aria-label":"Permalink to “Awards”"},"​")],-1)),e[14]||(e[14]=a("p",null,"Eventually we made our way to the auditorium for the closing ceremony. Now, as we walked in, I could feel the darkness creeping in, the bliss of sleep was calling me. Among my team, I made the poor decision to pull an all-nighter, and at that point I had been awake for over 36 hours. I really really should have taken an hour to sleep. When we sat down, I warned my team that I might be out cold. I stayed awake through the funny awards and the track awards. Then at one point I remember blinking after the Deepgram award and then it was lights out. I was completely out of it and deep asleep, and when I say deep sleep, I mean deep sleep. Then, much to our surprise, shock, and amazement, we ended up winning second place. By us, I mean the rest of my team. I was out so cold, I didn't realize it.",-1)),e[15]||(e[15]=a("p",null,"The following is pieced together from my friends. After our name was put on screen as the winner, my team had to spend 20 seconds shaking me until I woke up, and then when on stage, I apparently tried to hold the check vertically. I also had no clue why we were on stage until I sat back down and looked at the stage and at our comically large check. I believe it was only once we left the venue that I truly realized that we won.",-1)),e[16]||(e[16]=a("p",null,"At the end of the day, we pulled off what we thought impossible. We built a simple GPU in 24 hours and ran custom kernels on it. We even won HackMIT, but more important than all of that, the memories I made with my friends were the best part of it all.",-1)),t(o,{caption:"Our Awesome Team"},{default:n(()=>[...e[5]||(e[5]=[a("p",null,[a("img",{src:y,alt:""})],-1)])]),_:1}),e[17]||(e[17]=a("p",null,"<",-1))])}const D=l(b,[["render",k]]);export{U as __pageData,D as default};
